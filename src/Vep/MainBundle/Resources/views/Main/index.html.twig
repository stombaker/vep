{% extends '::base.html.twig' %}

{% block title %}Bienvenue sur le site de Voir &amp; Entendre{% endblock %}

{% block body %}
    <h1>Bienvenue sur le site de Voir et entendre</h1>
    
    <h2>Présentation</h2>
    
    <p>
        Bienvenue sur le site de l'association culturelle Voir &amp; Entendre à la Possonnière.
        Cette année encore, nous nous mobilisons pour vous proposer régulièrement des pièces de théâtre tout au long de l'année.
    </p>
    <p>
        Le mois de Juin arrive à grand pas et comme chaque année, les jeunes de l'atelier théâtre vous montreront leurs talents sous la forme de d'une pièce de théâtre mise en scène par Olivier Algourdin.
    </p>
    
    <p>
        En attendant, nous vous invitons à vous informer régulièrement sur les séances proposées.
    </p>
    
    {% if production is defined and production is not empty %}
        <h2>Prochainement</h2>
        
        <div class="row">
            <div class="col-sm-4">
                <img src="{{ asset(production.webPath) }}" alt="Poster de {{ production.title }}" class="poster" />
            </div>

            <div class="col-sm-8">
                <h3>{{ production.title }}</h3>
                <p>{{ production.content|nl2br }}</p>
                
                <p>
                    <a href="{{ path('vep_reservation_production_read', {'id': production.id}) }}" class="btn btn-primary">
                        Plus d'information
                        <i class="glyphicon glyphicon-share-alt"></i>
                    </a>
                </p>

                <h4>Liste des séances</h4>

                <ul class="list-unstyled">
                    {% for session in production.comingSessions %}
                        <li><a href="{{ path('vep_reservation_session_read', {'id': session.id}) }}">{{ session.date|date('d/m/Y \à H:i') }}</a></li>
                    {% else %}
                        <li class="text-muted">Aucune séance programmée pour le moment</li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    {% endif %}
{% endblock %}